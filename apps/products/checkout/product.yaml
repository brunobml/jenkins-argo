# git-files generator reads this whole file; its keys become template values
# (as real YAML types - note replicas is an int, featureFlags a list).
name: checkout
team: payments
replicas: 3
message: "checkout service"
featureFlags:
  - fast-path
  - one-click
resources:
  cpu: 25m
  memory: 48Mi
